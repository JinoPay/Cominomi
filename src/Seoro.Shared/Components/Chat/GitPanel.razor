@*
    GitPanel — 오른쪽 패널의 "Git" 모드 콘텐츠 호스트.
    VS Code 스타일 통합 뷰(GitView)를 단일 표시.
    sub-tab(Changes/Merge) 구조는 제거됨 — 머지 상태는 GitView 내 MergeStatusBanner 로 통합.
*@

<div class="git-panel">
    <GitView/>
</div>
